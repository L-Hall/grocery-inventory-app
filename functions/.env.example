# Firebase Functions Environment Variables
# Copy this file to .env for non-secret configuration

# NOTE: API keys and secrets are now managed through Google Cloud Secret Manager
# Do NOT put sensitive keys in this file

# Optional: Firebase project configuration (if not using default)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_DATABASE_URL=https://your-project.firebaseio.com

# Optional: Environment mode
# NODE_ENV=development

# Optional: Logging level
# LOG_LEVEL=info

# For local development with secrets:
# 1. Copy .secret.local.example to .secret.local
# 2. Add your API keys to .secret.local
# 3. NEVER commit .secret.local to version control

# For production:
# Use Firebase CLI to set secrets:
# firebase functions:secrets:set OPENAI_API_KEY